{
  "experiment": {
    "name": "biorag",
    "mode": "baseline"
  },
  "runtime": {
    "seed": 42,
    "num_workers": 0,
    "log_level": "INFO"
  },
  "paths": {
    "cache_dir": "data/interim",
    "artifact_dir": "outputs",
    "processed_dir": "data/processed"
  }
}
